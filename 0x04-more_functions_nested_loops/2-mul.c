#include "main.h"

/**
  * mul - Multiplies two integers
  * @m: The first number to multiply
  * @n: The second number to multiply with first number
  *
  * Return: The result of multiplication between m param and n param
  */
int mul(int m, int n)
{
	return (m * n);
}
