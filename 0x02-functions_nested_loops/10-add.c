#include "main.h"

/**
  * add - add an amount two values
  * @num1: the first number that is to be computed
  * @num2: the second number that is to be computed
  *
  * Return: int
  */
int add(int num1, int num2)
{
	return (num1 + num2);
}
